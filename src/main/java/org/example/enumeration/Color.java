package org.example.enumeration;

/**
 * Enum representing the possible colors of game pieces.
 * The colors include White color for testing purposes
 */
public enum Color {
    BLUE, RED, YELLOW, GREEN, WHITE
}
